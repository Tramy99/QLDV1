@model IEnumerable<QLDV1.Models.HoatDong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Hoạt Động</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenhd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.thoigiantc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ghichu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenhd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.thoigiantc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ghichu)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.mahd }) |
            @Html.ActionLink("Thông tin", "Details", new { id=item.mahd }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.mahd })
        </td>
    </tr>
}

</table>
